/**
 * @file main.cpp
 * @author Michael Meindl
 * @date 24.9.2016
 * @brief This file contains the main-function. Here the components are initialize and started as seperate threads.
 */
#include <iostream>
#include <pthread.h>
#include "CTest.h"

void* timerTask(void*)
{
	STimer& timer = STimer::getInstance();
	timer.init();
	timer.run();
	return nullptr;
}

int main()
{
	CTest test;
	pthread_t testID;
	pthread_create(&testID, NULL, testRun, NULL);

	pthread_join(testID, NULL);
	std::cout << "Hello World" << std::endl;
	return 0;
}
