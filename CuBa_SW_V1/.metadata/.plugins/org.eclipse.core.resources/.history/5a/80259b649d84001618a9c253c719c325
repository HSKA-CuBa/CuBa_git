#include "CThread.h"
#include "CRxTask.h"
#include "CComponent.h"

int main()
{
	CComponent comp;
	CThread compThred(&comp);
	CRxTask rxTask;
	CThread rxThread(&rxTask);
	rxThread.start();

	rxThread.join();
	return 0;
}
